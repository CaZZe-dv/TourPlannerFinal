<UserControl x:Class="TourPlanner.UI.Views.HeaderBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.UI.Views"
             mc:Ignorable="d" 
             x:Name="root"
             d:DesignHeight="40" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Content="File" FontSize="24" Command="{Binding FileCommand, ElementName=root}"/>
        <Button Grid.Column="1" Content="Edit" FontSize="24" Command="{Binding EditCommand, ElementName=root}"/>
        <Button Grid.Column="2" Content="Options" FontSize="24" Command="{Binding OptionsCommand, ElementName=root}"/>
        <Button Grid.Column="5" Content="Help" FontSize="24" Command="{Binding HelpCommand, ElementName=root}"/>
        
    </Grid>
</UserControl>
