<UserControl x:Class="TourPlanner.UI.Views.LocationPickerViewTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.UI.Views"
             mc:Ignorable="d" 
             x:Name="root"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
        <TextBlock Text="From:" Margin="10"/>
        <TextBox Text="{Binding From, ElementName=root, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="10"/>
        <TextBlock Text="To:" Margin="10"/>
        <TextBox Text="{Binding To,ElementName=root, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="10"/>
    </StackPanel>
</UserControl>
